{
  "name": "api-docs-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for running frontend and backend concurrently.",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
